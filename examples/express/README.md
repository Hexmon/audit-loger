# Express Example (TypeScript)

This example shows a TypeScript Express integration with audit context extraction
and file-backed JSONL logging.

## Run
1) Install dependencies at the repo root.
2) Build the packages: `pnpm -r build`
3) Run the example with ts-node or compile it in your app.
